public class ConvertClass {
    public String convert(String s, int numRows) {
        if (s.length() <= numRows)
            return s;
        int rows = s.length()/numRows+s.length()/numRows-1;
        char[][] arr1 = new char[numRows][rows];
        for (int i = 0; i < s.length(); i++) {

        }
        return "";
    }
}
